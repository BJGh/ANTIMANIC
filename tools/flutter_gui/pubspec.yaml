name: mcp_flutter_gui
description: Minimal Flutter GUI skeleton for MCP Dart Copilot integration
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  web_socket_channel: ^2.4.0
  get_it: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true