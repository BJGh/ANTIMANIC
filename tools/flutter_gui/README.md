Flutter GUI skeleton for MCP Dart Copilot

Run (when Flutter SDK is installed):
  cd tools/flutter_gui
  flutter run

This is a minimal starting point — implement MCP client code to talk to
`tools/mcp_dart_copilot` HTTP/WebSocket server.
