MCP Dart Copilot (placeholder)

What this is:
- Minimal skeleton for an MCP (Model Context Protocol) server implemented in Dart.
- Intended as the `MCPDartCopilot` component (server + integration points).

Next steps:
- Implement MCP transport (HTTP / WebSocket) and message handlers.
- Wire into workspace CI and add example Flutter client in `/tools/flutter_gui/`.
